---
import { renderCover } from '@/lib/cover';
import type { CATEGORIES } from '@/content/config';

interface Props {
  slug: string;
  category: (typeof CATEGORIES)[number];
  width?: number;
  height?: number;
  title?: string;
  class?: string;
}

const { slug, category, width, height, title, class: className = '' } = Astro.props;
const svg = renderCover({ slug, category, width, height, title });
---
<div class={`overflow-hidden ${className}`} set:html={svg} />

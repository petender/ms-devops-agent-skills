{
  "name": "ms-devops-agent-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Community gallery of dual-format (Claude + Copilot) DevOps agent skills for trainers and learners.",
  "scripts": {
    "dev": "npm run import && astro dev",
    "build": "npm run validate && npm run import && astro build",
    "preview": "astro preview",
    "import": "node scripts/import-submissions.mjs",
    "validate": "node scripts/validate-submission.mjs",
    "new-skill": "node scripts/new-skill.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.0",
    "@astrojs/sitemap": "^3.2.0",
    "@astrojs/tailwind": "^6.0.0",
    "astro": "^5.5.0",
    "tailwindcss": "^3.4.10",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "jszip": "^3.10.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
